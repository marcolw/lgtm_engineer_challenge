apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lgtm-demo-apps
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/marcolw/lgtm_engineer_challenge.git # <-- UPDATE YOUR HUB USERNAME/REPO
    targetRevision: HEAD
    path: k8s/demo-apps
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: default # Deploy the actual apps into the 'default' namespace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true