apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-service
  labels:
    app: go-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-service
  template:
    metadata:
      labels:
        app: go-service
    spec:
      containers:
      - name: go-service
        image: docker.io/marcoliew/lgtm-go-service:v0.1.0 # <-- UPDATE YOUR HUB USERNAME/TAG
        ports:
        - containerPort: 8080
        env:
        # Crucial for OTel SDK: Points to the Collector Service
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.monitoring.svc.cluster.local:4317"
        - name: OTEL_SERVICE_NAME
          value: "go-lgtm-service"
---
apiVersion: v1
kind: Service
metadata:
  name: go-service
spec:
  selector:
    app: go-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP # Internal service access