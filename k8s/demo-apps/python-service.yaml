apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-service
  labels:
    app: python-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-service
  template:
    metadata:
      labels:
        app: python-service
    spec:
      containers:
      - name: python-service
        image: docker.io/marcoliew/lgtm-python-service:v0.1.0 # <-- UPDATE YOUR HUB USERNAME/TAG
        ports:
        - containerPort: 8080
        env:
        # Crucial for OTel SDK: Points to the Collector Service
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.monitoring.svc.cluster.local:4317"
        - name: OTEL_SERVICE_NAME
          value: "python-lgtm-service"
---
apiVersion: v1
kind: Service
metadata:
  name: python-service
spec:
  selector:
    app: python-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP # Internal service access